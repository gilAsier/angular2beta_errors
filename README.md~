## Run Angular 2 app with jspm and babel as a transpiler
1.  `npm install -g jspm live-server`
2.  Clone this repo
3.  `jspm install`
4.  Run `live-server` and observe the results

### Bundle project
`npm run build`

MIT License
